# DataForge 1.0 — matriz de implementación y evidencia

**Estado:** activa  
**Fuente:** RFC-0001 §45 y Definition of Done general  
**Regla:** una casilla solo pasa a completada cuando existe código, prueba del
criterio completo y evidencia de la puerta de calidad. Una release/tag remoto
no se presupone a partir de cambios locales.

## Base ya demostrada localmente

| Incremento | Capacidad | Evidencia actual |
| --- | --- | --- |
| M0.0–M0.1 | Fundación y pipeline seguro | Suite workspace, hardening Windows y pipeline create→verify |
| M0.2 | Estructura, contextos, reglas, revisión | Suite focal, UI accesible y corpus final de 100.000 archivos con `COMPLETED` |
| M0.3 | Similitud y versionado | FastCDC/MinHash/LSH + score exacto, persistencia sellada, CLI/fachada/UI, benchmark 256 MiB y suite congelada 312/0/2 |

Todo lo anterior está publicado en `main` con CI verde y liberado como
1.0.0. Publicar la release desde su borrador sigue siendo un acto humano
deliberado (ADR-0039): esta matriz describe el producto, no finge estado
remoto.

## Trabajo obligatorio hasta 1.0

| Hito | Contrato verificable | Estado |
| --- | --- | --- |
| M0.3 Similarity and Versioning | FastCDC streaming versionado; chunks BLAKE3; MinHash+LSH acotados; similitud exacta ponderada; candidatos y linaje explicables; variantes sintéticas; umbrales configurables; benchmark y límites de almacenamiento; vista de versiones | Completado y publicado en `main` con CI verde |
| M0.4 Content Intelligence | Extractores aislados para PDF/DOCX/TXT/HTML/EML/ZIP; texto normalizado; adjuntos e hilos básicos; ZIP hostil acotado; índice Tantivy reconstruible; Parquet/DataFusion y SQL analítico con ruta/contexto | Completado localmente; 257 tests Rust + 11 UI, gates focales verdes; publicado en `main` con CI verde |
| M0.5 Media Intelligence | Fingerprints de imagen/audio/vídeo; pHash, Chromaprint y keyframes; recompression/resize/transcoding sintéticos; revisión multimedia; ninguna acción automática destructiva | Completado localmente (ADR-0032, migración 0016, E2E con worker aislado y rediciones sintéticas; suite y gates verdes); publicado en `main` con CI verde |
| M0.6 Plugin Ecosystem | ABI WIT versionada; Wasmtime/WASI sin filesystem por defecto; capacidades, timeout y memoria; registro/hash/firma; compatibilidad y plugins de ejemplo documentados | Completado localmente (ADR-0033, migración 0017, registro persistido re-verificado, runs sellados, E2E firmado real; deuda: SubjectText sin poblar y revocación futura); publicado en `main` con CI verde |
| M0.7 Assisted Intelligence | Proveedor abstracto local y cloud opt-in; datos visibles/redactables; JSON Schema validado; auditoría; IA sin capacidad de ejecutar; riesgo recalculado; corpus de prompt injection documental | Completado localmente (ADR-0034, migración 0018, BYOK en Credential Manager, transportes Anthropic/OpenAI en el borde, consentimiento por digest, ruta air-gapped, E2E determinista; deuda: caso de uso inicial y UI pendiente); publicado en `main` con CI verde |
| M0.8 Cross-platform and Scale | Lectura/escritura segura experimental en Linux/macOS; 1M+ entradas medido; NAS endurecido; snapshots incrementales, cache y daemon experimental | Parcial: 1M+ medido (1.093.705 operaciones ejecutadas y verificadas `COMPLETED`, `docs/testing/m0.8-scale-1m.md`), job CI Linux experimental VERDE (compilación completa, clippy estricto y suite neutral en ubuntu; 11 fronteras Windows-first fijadas como comportamiento en 14 rondas) y snapshots incrementales opt-in (ADR-0035, migración 0019); NAS endurecido (ADR-0036); **caché de hash analizado y descartado como estructura propia**: un caché keyed por el fingerprint físico completo es redundante con el reuso incremental por ruta (cualquier segunda referencia a un inodo perturba su ChangeTime, que va en el fingerprint) y una clave más débil reintroduciría la sustitución que el v2 previene; un caché entre proyectos exigiría un almacén global con su propio modelo de amenazas (diferido). Pendiente: daemon experimental |
| M0.9 Stabilization | Contratos/schema/API/ABI congelados; migraciones y compatibilidad; threat model final; builds reproducibles; SBOM; firma; fuzzing; corpus completo; UX y accesibilidad finales; manual de usuario | Completado: contratos congelados con test de regresión (ADR-0037, `df-facade::frozen_contracts`); compatibilidad de migraciones probada; threat model final consolidado hasta M0.8; manual de usuario completo; SBOM CycloneDX 1.5 reproducible; accesibilidad del escritorio documentada y probada (`docs/accessibility.md`); fuzzing de los parsers de entrada no confiable (`fuzz/`, cuatro dianas cargo-fuzz + job de CI Linux experimental); builds reproducibles: no-determinismo del `TimeDateStamp` PE encontrado, arreglado y verificado en CI (ADR-0038); workflow de release en borrador con SBOM verificado, checksums y doble build de reproducibilidad bloqueante; firma keyless Sigstore implementada (ADR-0039); UI del escritorio rediseñada para usuarios no técnicos. El pase de lector de pantalla real + axe queda como deuda post-1.0 declarada |
| M1.0 Stable Reconstruction Platform | Todas las garantías anteriores integradas: origen inmutable, plan versionado, copia verificada, reanudación, formatos estables, plugins compatibles, búsqueda, perfiles, informes, seguridad documentada, builds firmadas y soporte de migración | Integrado: mapa garantía→evidencia en `docs/release/m1.0-acceptance.md` (Windows-first, daemon post-1.0, firma keyless — decisiones con veto abierto hasta el tag). Bump a 1.0.0 aplicado y publicado (PR #29); el tag `v1.0.0` dispara el build firmado y la release en borrador |

## Evidencia transversal exigida por cada hito

1. Migración append-only con checksum, compatibilidad desde bases anteriores y
   pruebas de constraints/triggers.
2. Contratos de dominio y JSON versionados, sin reinterpretar evidencia
   histórica.
3. Algoritmos deterministas, acotados y con configuración incluida en la
   evidencia que producen.
4. Fallo cerrado ante fuente modificada, formato hostil, límite agotado o
   plataforma sin garantía equivalente.
5. CLI, facade y escritorio consumen el mismo motor; la UI no decide.
6. `fmt`, clippy estricto, tests, builds, auditorías de dependencias y pruebas
   end-to-end verdes.
7. CHANGELOG, README, ADR, arquitectura y threat model actualizados antes de
   declarar completado el incremento.
