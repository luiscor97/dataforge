{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set of the main window: the df-facade commands plus a user-initiated folder picker (dialog open). No ambient filesystem access.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
