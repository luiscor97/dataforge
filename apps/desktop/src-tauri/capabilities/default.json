{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set of the main window. The UI only talks to the df-facade commands; it has no filesystem access of its own.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
